import styled from "styled-components";

const Container = () => {
  const Container = styled.div`
    width: 100%;
    height: 100vh;
    background: blue;
  `
  return (
    <Container />
  )
};

export default Container;